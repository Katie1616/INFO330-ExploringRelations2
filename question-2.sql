-- Show the sales agent's full name and invoices associated with each sales agent.
 select employees.FirstName, employees.LastName, invoices.InvoiceID from employees, customers, invoices where invoices.customerID = customers.customerID and customers.supportrepID = employees.employeeID;